# Lasso模型超参数选择方法详解

## 一、超参数选择方法

所有模型都使用 **`LassoCV`**（Lasso with Cross-Validation）来自动选择最优的正则化参数 `alpha`。

### 1.1 LassoCV的工作原理

`LassoCV` 是 scikit-learn 提供的类，它：
1. **自动进行交叉验证**：在训练数据上进行K折交叉验证
2. **搜索最优alpha**：在默认的alpha范围内搜索（通常是对数空间：1e-5 到 1e2）
3. **选择最优值**：选择交叉验证得分最高的alpha值
4. **使用最优alpha训练最终模型**：在整个训练集上使用最优alpha重新训练

### 1.2 当前实现

所有模型使用以下设置：

```python
LassoCV(
    cv=min(5, len(data)//2),  # 交叉验证折数
    random_state=42,           # 随机种子
    max_iter=2000              # 最大迭代次数
)
```

## 二、交叉验证折数（cv参数）

### 2.1 自适应CV折数

**公式**：`cv = min(5, len(data)//2)`

**含义**：
- 默认最多使用 **5折交叉验证**
- 如果样本数较少，使用更少的折数：`len(data)//2`
- 确保每折至少有足够的样本

### 2.2 实际CV折数示例

| 样本数 | CV折数 | 说明 |
|--------|--------|------|
| 10-19 | 5 | 每折2-3个样本 |
| 20-29 | 5 | 每折4-5个样本 |
| 30-39 | 5 | 每折6-7个样本 |
| 40+ | 5 | 每折8+个样本 |
| < 10 | `len(data)//2` | 样本太少时减少折数 |

**示例**：
- 样本数 = 24 → CV = min(5, 24//2) = min(5, 12) = **5折**
- 样本数 = 47 → CV = min(5, 47//2) = min(5, 23) = **5折**
- 样本数 = 8 → CV = min(5, 8//2) = min(5, 4) = **4折**

### 2.3 Delta模型中的嵌套CV

在Delta模型中，使用了**嵌套交叉验证**（Nested Cross-Validation）：

```python
# 外层CV：用于OOF预测
kf = KFold(n_splits=k, shuffle=True, random_state=42)

for tr_idx, te_idx in kf.split(X):
    # 内层CV：用于选择alpha
    lcv = LassoCV(cv=min(5, max(2, len(tr_idx)//2)), random_state=42, max_iter=2000)
    lcv.fit(X_tr_sc, y_tr)
```

**两层CV的目的**：
- **外层CV**：将数据分成训练集和测试集，用于生成Out-of-Fold预测
- **内层CV**：在训练集上选择最优alpha，避免过拟合

## 三、Alpha搜索范围

### 3.1 默认搜索范围

`LassoCV` 默认在以下alpha范围内搜索：
- **搜索数量**：默认搜索 **100个alpha值**（`n_alphas=100`）
- **范围计算**：根据数据自动计算alpha范围
  - 最大alpha：使得所有系数为0的alpha值
  - 最小alpha：最大alpha的1/1000（或更小）
  - 在最大和最小alpha之间，在对数空间均匀分布100个值
- **选择标准**：选择交叉验证得分最高的alpha

**实际范围示例**（基于测试）：
- 最大alpha：约 0.01 - 0.4（取决于数据）
- 最小alpha：约 0.0001 - 0.001（最大alpha的1/1000）
- 搜索的alpha值在对数空间均匀分布

### 3.2 Alpha的含义

- **alpha = 0**：无正则化，等价于普通最小二乘（OLS）
- **alpha 增大**：正则化增强，更多系数被压缩到0（特征选择）
- **alpha 很大**：所有系数都接近0（欠拟合）

### 3.3 实际选择的Alpha值

从运行结果看，选择的alpha值通常在：
- **范围**：0.0001 到 0.06
- **趋势**：随着样本数增加，alpha值通常减小（模型复杂度增加）

## 四、超参数选择流程

### 4.1 标准模型（非Delta）

```
1. 准备数据：X (特征), y (目标变量)
2. 标准化特征：StandardScaler().fit_transform(X)
3. LassoCV自动选择alpha：
   a. 将数据分成K折（K = min(5, len(data)//2)）
   b. 对每个候选alpha值：
      - 在K-1折上训练模型
      - 在剩余1折上评估性能
      - 计算K折的平均得分
   c. 选择平均得分最高的alpha
4. 使用最优alpha在整个数据集上训练最终模型
5. 返回训练好的模型（包含最优alpha）
```

### 4.2 Delta模型（嵌套CV）

```
1. 准备数据：X (特征), y (目标变量)
2. 外层CV：将数据分成K折（用于OOF预测）
3. 对每一折：
   a. 训练集 = 其他K-1折
   b. 测试集 = 当前折
   c. 在训练集上：
      - 内层CV：LassoCV选择最优alpha
      - 使用最优alpha训练模型
   d. 在测试集上预测（OOF预测）
4. 所有OOF预测组合 = 最终预测
```

## 五、超参数选择的一致性

### 5.1 所有模型使用相同的设置

| 参数 | 值 | 说明 |
|------|-----|------|
| **CV折数** | `min(5, len(data)//2)` | 自适应，最多5折 |
| **Random State** | 42 | 确保可复现性 |
| **Max Iter** | 2000 | 最大迭代次数 |
| **Alpha搜索** | 默认范围 | LassoCV自动搜索 |
| **选择标准** | 交叉验证得分 | 默认使用MSE |

### 5.2 为什么使用自适应CV？

1. **小样本问题**：当样本数少时，5折CV可能导致每折样本太少
2. **稳定性**：确保每折有足够的样本进行可靠的评估
3. **灵活性**：根据数据量自动调整，无需手动设置

## 六、超参数选择示例

### 6.1 季度模型示例

```python
# 2020 Q1: 24个样本
lasso = LassoCV(cv=min(5, 24//2), random_state=42, max_iter=2000)
# CV = min(5, 12) = 5折
# LassoCV会在5折CV上搜索最优alpha
# 最终选择的alpha存储在 lasso.alpha_ 中
```

### 6.2 年度模型示例

```python
# 2020年: 24个样本
lasso = LassoCV(cv=min(5, 24//2), random_state=42, max_iter=2000)
# CV = min(5, 12) = 5折
# 与季度模型使用相同的CV策略
```

### 6.3 Delta模型示例

```python
# 外层CV：用于OOF预测
kf = KFold(n_splits=10, shuffle=True, random_state=42)

for tr_idx, te_idx in kf.split(X):
    # 内层CV：在训练集上选择alpha
    lcv = LassoCV(cv=min(5, max(2, len(tr_idx)//2)), random_state=42, max_iter=2000)
    # 如果训练集有50个样本，CV = min(5, max(2, 25)) = 5折
    # 如果训练集有10个样本，CV = min(5, max(2, 5)) = 5折
    # 如果训练集有4个样本，CV = min(5, max(2, 2)) = 2折
```

## 七、超参数选择的影响

### 7.1 实际选择的Alpha值

从实际运行结果看：

**季度模型Alpha值**：
- **范围**：0.000226 到 0.230929
- **平均值**：0.027406
- **趋势**：随时间（样本数增加）而减小
  - 早期（2020 Q1）：alpha ≈ 0.015-0.23（样本少，保守）
  - 后期（2025 Q2）：alpha ≈ 0.0002-0.004（样本多，允许更多特征）

**年度模型Alpha值**：
- **范围**：0.002142 到 0.020036
- **平均值**：0.008037
- **趋势**：2020年最高（0.020），之后稳定在0.002-0.012之间

### 7.2 Alpha值与样本数的关系

| 样本数 | 典型Alpha值 | 选择的特征数 | R² | 说明 |
|--------|------------|-------------|-----|------|
| 小（20-30） | 0.01-0.04 | 2-4个 | 0.4-0.7 | 保守，选择少量重要特征 |
| 中（40-80） | 0.003-0.01 | 6-8个 | 0.5-0.6 | 平衡，选择较多特征 |
| 大（100+） | 0.001-0.005 | 8-9个 | 0.5-0.6 | 宽松，允许使用几乎所有特征 |

**趋势**：
- ✅ 样本数越多，alpha越小（允许更多特征）
- ✅ 样本数越少，alpha越大（更保守的特征选择）
- ✅ 这符合统计学习理论：更多数据允许更复杂的模型

### 7.3 为什么Alpha随时间减小？

从结果看，alpha值随时间（样本数增加）而减小：
- **2020 Q1**: alpha ≈ 0.015-0.23（样本少，保守）
- **2025 Q2**: alpha ≈ 0.0002-0.004（样本多，允许更多特征）

这符合预期：更多数据允许模型使用更多特征而不易过拟合。

## 八、总结

### 8.1 超参数选择方法

1. **方法**：使用 `LassoCV` 自动选择alpha
2. **CV折数**：自适应 `min(5, len(data)//2)`
3. **搜索范围**：LassoCV默认范围（对数空间）
4. **选择标准**：交叉验证平均得分最高
5. **随机性**：`random_state=42` 确保可复现

### 8.2 关键特点

- ✅ **自动化**：无需手动指定alpha值
- ✅ **自适应**：根据数据量自动调整CV折数
- ✅ **一致性**：所有模型使用相同的选择策略
- ✅ **可复现**：固定random_state确保结果可复现

### 8.3 与手动调参的对比

| 方法 | 优点 | 缺点 |
|------|------|------|
| **LassoCV（当前）** | 自动化、自适应、无需手动调参 | 搜索范围固定，可能不是最优 |
| **手动GridSearch** | 可以自定义搜索范围 | 需要手动指定，计算量大 |
| **贝叶斯优化** | 更高效的搜索 | 实现复杂，需要额外库 |

**当前方法的选择**：LassoCV是平衡效率和效果的最佳选择，适合本研究的场景。

---

*参考文档*：
- scikit-learn LassoCV: https://scikit-learn.org/stable/modules/generated/sklearn.linear_model.LassoCV.html
- 所有模型文件中的LassoCV调用

